<?xml version="1.0" encoding="UTF-8"?>
<openerp>
   <data noupdate="1">
        <record id="pdsl_fetchmail_subscription_confirm_email_template" model="email.template">
            <field name="name">Confirm subscription</field>
            <field name="email_from"><![CDATA[${object.company_id.name} <${object.company_id.email|safe}>]]></field>
            <field name="subject">Welcome to Minarca! Please Confirm Your Email</field>
            <field name="email_to">${object.email_from|safe}</field>
            <field name="lang">${ctx.get('lang') or object.partner_id.lang}</field>
            <field name="model_id" search="[('model','=','crm.lead')]" model="ir.model"/>
            <field name="auto_delete" eval="False"/>
            <field name="body_html"><![CDATA[
<p>Welcome to Minarca!<p>

<p>Thanks for signing up for Minarca! To used our backup service, please verify your email address by clicking the link below.</p>

<p><a href="http://www.patrikdufresne.com/cgi-bin/support.cgi?action=confirm&redirect=/fr/minarca/subscribe-email-confirm.html&messageid=${object.message_ids[0].message_id}">CONFIRM EMAIL</a></p>

<p>Note that this link will expire in 24 hours if it's not activated.</p>

<p>Request ID: ${object.id}</p>

<p>Thank you,<br>
Patrik Dufresne Service Logiciel inc.</p>

<p>--</p>
<p>Original message received:</p>

<pre>
${object.description}
</pre>]]></field>
        </record>
        <record id="pdsl_fetchmail_support_confirm_email_template" model="email.template">
            <field name="name">Confirm support</field>
            <field name="email_from"><![CDATA[${object.company_id.name} <${object.company_id.email|safe}>]]></field>
            <field name="subject">Confirm ${object.name} [ref. ${object.id}]</field>
            <field name="email_to">${object.email_from|safe}</field>
            <field name="lang">${ctx.get('lang') or object.partner_id.lang}</field>
            <field name="model_id" search="[('model','=','crm.lead')]" model="ir.model"/>
            <field name="auto_delete" eval="False"/>
            <field name="body_html"><![CDATA[

<p>We have received your request and we will respond as quickly as possible.<p>

<p>We usually try to respond to requests for assistance within 48 hours, Monday to Friday.</p>

<p>Request ID: ${object.id}</p>

<p>Thank you,<br>
Patrik Dufresne Service Logiciel inc.</p>

<p>--</p>
<p>Original message received:</p>

<pre>
${object.description}
</pre>]]></field>
        </record>
        
        <record id="pdsl_fetchmail_issue_confirm_email_template" model="email.template">
            <field name="name">Confirm support</field>
            <field name="email_from"><![CDATA[${object.company_id.name} <${object.company_id.email|safe}>]]></field>
            <field name="subject">Confirm ${object.name} [ref. ${object.id}]</field>
            <field name="email_to">${object.email_from|safe}</field>
            <field name="lang">${ctx.get('lang') or object.partner_id.lang}</field>
            <field name="model_id" search="[('model','=','project.issue')]" model="ir.model"/>
            <field name="auto_delete" eval="False"/>
            <field name="body_html"><![CDATA[

<p>We have received your request and we will respond as quickly as possible.<p>

<p>We usually try to respond to requests for assistance within 48 hours, Monday to Friday.</p>

<p>Request ID: ${object.id}</p>

<p>Thank you,<br>
Patrik Dufresne Service Logiciel inc.</p>

<p>--</p>
<p>Original message received:</p>

<pre>
${object.description}
</pre>]]></field>
        </record>
        
    </data>
</openerp>
